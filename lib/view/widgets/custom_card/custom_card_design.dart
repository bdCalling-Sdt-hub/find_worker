import 'package:flutter/material.dart';

class CustomCardDesign extends StatelessWidget {
  const CustomCardDesign({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
